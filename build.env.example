# build.env — Local machine config (gitignored, ไม่ commit)
#
# วิธีใช้:
#   1. copy ไฟล์นี้ → build.env
#   2. แก้ METAEDITOR ให้ตรงกับเครื่องของคุณ
#   3. รัน: bash build.sh
#
# ถ้าไม่มี build.env → build.sh จะ auto-detect path จากตำแหน่งทั่วไป

METAEDITOR="H:/Program Files (x86)/MetaTrader 4 EXNESS/metaeditor.exe"
